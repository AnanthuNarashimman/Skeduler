# Ignore top-level node_modules (project-level)
node_modules/

# Also ignore the nested frontend package node_modules
skeduler/node_modules/

# Optional: ignore package-lock if you don't want it tracked
# package-lock.json
